# Mitigate supply chain attacks by requiring packages to have been
# published for at least 7 days before they can be installed.
# Requires pip >= 26.1. Duration uses ISO 8601 format.
[install]
uploaded-prior-to = P7D
