---
name: Tests
"on":
  push:
  pull_request:

jobs:

  tests:
    name: Run full installation
    runs-on: macos-11.0
    steps:
      - uses: actions/checkout@v2.3.4
      - name: Run install
        run : ./install.sh
